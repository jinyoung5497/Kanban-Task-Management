import { render } from '@testing-library/react'
import TaskModal from './TaskModal'

describe(TaskModal, () => {
  it('testing something', () => {
    const { container } = render(<TaskModal />)
    const getArray = getByRole()
  })
})
